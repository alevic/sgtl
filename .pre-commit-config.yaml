repos:
  - repo: local
    hooks:
      - id: check-migrations
        name: Ensure Alembic migration when models change
        entry: scripts/check_migrations.py
        language: python
        pass_filenames: false
